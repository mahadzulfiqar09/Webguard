# Webguard
WebGuard is a modular web application vulnerability scanner built in Python. It crawls a target site to map its attack surface, then tests for missing security headers, insecure cookies, reflected XSS, and SQL injection  generating a severity-rated report at the end
